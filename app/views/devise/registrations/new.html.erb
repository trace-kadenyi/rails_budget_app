<div class="user_form">
  <h2>Sign up</h2>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: false }, html: { method: :post }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field mb-4">
      <%= f.text_field :name, autofocus: true, placeholder: "Name", class:'form-control' %>
    </div>

    <div class="field mb-4">
      <%= f.email_field :email, autofocus: true, placeholder: "Email", autocomplete: "email", class:'form-control' %>
    </div>

    <div class="field mb-4">
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %><br />
      <%= f.password_field :password, placeholder: "Password", autocomplete: "new-password", class:'form-control' %>
    </div>

    <div class="field mb-4">
      <%= f.password_field :password_confirmation, placeholder: "Password_confirmation", autocomplete: "new-password", class:'form-control' %>
    </div>

    <div class="actions mb-4">
      <%= f.submit "Sign up", class:'form-control'%>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
