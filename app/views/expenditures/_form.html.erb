<%= form_with(model: expenditure, url: category_expenditures_path, local: true) do |form| %>
  <% if expenditure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expenditure.errors.count, "error") %> prohibited this expenditure from being saved:</h2>

      <ul>
        <% expenditure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :name, class:'form-control mb-3', placeholder: 'Enter name'%>
  </div>

  <div>
    <%= form.text_field :amount, class:'form-control mb-3', placeholder: "Amount" %>
  </div>

  <div class="field mb-4">
    <%= form.label :category, class: "block text-gray-700 text-m font-bold mb-2" %>
    <%= form.collection_select :category_id, 
    Category.where("user_id = ?", current_user.id).order(:name), :id, :name, class:'form-control mb-3' %>
  </div>

  <div>
    <%= form.submit 'Save expenditure', class:'form-control mb-3'%>
  </div>
<% end %>
