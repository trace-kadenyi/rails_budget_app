<%= form_with model: [@category, @category.categories_expenditure.create] do |form| %>
  <% if categories_expenditure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize categories_expenditure.errors.count, "error" %> prohibited this categories_expenditure from being saved:</h2>

      <ul>
        <% categories_expenditure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="mb-3">
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>

  <%= form.hidden_field :category_id, value: category_id.id  %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
